{
  "name": "mui_key_error_test",
  "version": "0.0.0",
  "description": "Prototype for sharing mui key error situations",
  "author": "qsoo",
  "license": "UNLICENSED",
  "scripts": {
    "build": "pnpm --parallel --filter \"./**\" build",
    "lint": "pnpm --parallel -r --filter \"./**\" run lint",
    "lint-fix": "pnpm --parallel -r --filter \"./**\" run lint-fix",
    "preview": "pnpm --parallel --stream -r --filter ./** run preview",
    "build:remotes": "pnpm --parallel --filter \"./remotes/**\" build",
    "preview:remotes": "pnpm --parallel --filter \"./remotes/**\" preview",
    "dev:hosts": "pnpm --filter \"./host\" dev"
  }
}
